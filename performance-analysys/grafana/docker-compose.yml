services:
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./data:/data
      - ./provisioning:/etc/grafana/provisioning
    environment:
      - GF_PLUGINS_PREINSTALL=grafana-image-renderer, frser-sqlite-datasource
    restart: unless-stopped

volumes:
  grafana-data:
