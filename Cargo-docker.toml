[workspace]
members = ["core", "cli", "daemon"]
resolver = "2"

[workspace.dependencies]
# Shared dependencies across the workspace
# No one yet

[profile.dev]
opt-level = 0
debug = true

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
